<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:menu="http://schemas.android.com/tools" xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
            android:id="@+id/menu_help"
            android:icon="@drawable/ic_action_help_outline"
            android:title="@string/introduction"
            app:showAsAction="ifRoom"/>

    <item
            android:id="@+id/menu_daynight"
            android:icon="@drawable/ic_action_settings_brightness"
            android:title="@string/daynight"
            app:showAsAction="ifRoom"/>

    <item
            android:id="@+id/menu_share"
            android:icon="@drawable/ic_social_share"
            android:title="@string/share"
            app:showAsAction="ifRoom"/>

</menu>
